# 花牌记分器 V0.2.8

## Description

   - 花牌记分器用于2-6人的花牌记分
   - 主要帮助统计玩家分数，核对本轮数据
   - 用html、css3、JavaScript实现，可在线使用，也可下载离线使用
   - [点击使用花牌记分器](https://kit.aibox.nl/HuaPaiScore/HuaPai.html)

## Usage

   1. 填写**玩家名字**
   2. 根据实际得分情况，填写本轮得分
      - 例如：-10，10，+50
   3. 点击 **'结算得分'** 记分器会自动**核算本轮得分是否正确**
      - 当发现本轮得分错误，在**消息盒子**中会提示
      - 当本轮得分正确无误，记分器会将得分**核算到累计得分中**

   <br/>

   - 点击 **'重置'** 可重置**累计得分**和**本轮得分**
   - 点击 **'历史记录'** 可查看历史结算记录
   - 点击 **'更多'** > **'关于'** 可查看软件信息和功能列表

## 功能列表

   - **基础功能**：记分、结算差错、重置分数、数据本地存储
   - **新增功能（V0.2.8）**：使用次数统计（显示我的结算次数和软件累计结算次数）
   - **新增功能（V0.2.7）**：用户昵称、用户信息面板（显示昵称和用户ID）
   - **新增功能（V0.2.6）**：访问统计（点击结算时记录玩家数据和设备信息）
   - **扩展功能**：添加到桌面（PWA）、更多菜单

## Update

   - **V0.2.8**
      - **新增使用次数统计功能**
         - 在"关于"面板中显示"我的结算次数"
         - 在"关于"面板中显示"软件累计结算次数"
         - 每次结算时自动更新使用次数
         - 累计次数为所有用户共用统计
      - **优化远端存储数据格式**
         - 修复查询失败时存储错误数据的问题
         - 优化API响应格式处理
         - 修复CORS跨域支持
   - **V0.2.7**
      - **新增用户昵称功能**
         - 首次打开应用时强制输入昵称
         - 昵称存储到本地，后续自动读取
         - 支持在用户信息面板中修改昵称
      - **新增用户信息面板**
         - 在"更多"菜单中新增"用户信息"按钮
         - 显示用户昵称和用户ID（Canvas指纹）
         - 支持修改昵称
      - **优化远端存储ID格式**
         - ID格式变为：HuaPaiScore_<昵称>_<指纹ID>
         - 方便识别不同用户
   - **V0.2.6**
      - **新增访问统计功能**
         - 点击"结算得分"时自动记录游戏数据
         - 记录内容包括：玩家名字、累计得分、本轮分数
         - 同时记录设备信息：操作系统、浏览器类型和版本
         - 使用 Canvas 指纹作为用户标识
         - 数据通过 API 存储到服务器
   - **V0.2.5**
      - **新增用户标识功能**
         - 使用 Canvas 指纹技术生成设备唯一标识
         - 将指纹转换为 MD5 哈希值（32位字符串）
         - 在"关于"面板中显示用户标识
         - 可用于识别不同设备
   - **V0.2.4**
      - **新增轮次显示功能**
         - 在记分表格上方显示当前轮次
         - 每次结算成功后轮次自动+1
         - 点击重置后轮次回到第1轮
      - **新增得分趋势图功能**
         - 在历史记录中点击皇冠图标可查看得分趋势折线图
         - 图表支持多玩家得分对比
         - 实时显示玩家排名和最终得分
      - **新增统计信息展示**
         - 累积最高/最低分记录及对应玩家
         - 单轮最高/最低得分及对应玩家
         - 分数颜色区分：非负数金色，负数红色
      - **新增皇冠排名动画**
         - 前三名玩家名字上方显示皇冠图标
         - 金牌（金色）、银牌（银色）、铜牌（铜色）
         - 金牌皇冠带脉冲发光和粒子特效动画
   - **V0.2.3**
      - **新增历史记录功能**
         - 每次结算成功后自动保存记录到本地
         - 记录内容包括：玩家名字、累计得分、本轮得分、结算时间
         - 文件名按结算时间命名（年月日_时分秒格式）
         - 点击"历史记录"按钮可查看所有历史记录
         - 支持查看每场游戏的详细得分信息
         - 支持删除不需要的历史记录
         - 点击"重置"后，下次结算将创建新的历史记录文件
      - **新增动画效果**
         - 弹窗打开动画（缩放淡入效果）
         - 消息盒子成功/错误动画（成功时脉冲缩放，错误时左右摇摆+颜色变化）
         - 分数变化动画（得分变化时脉冲效果）
         - 仙鹤轻微浮动动画
         - 按钮悬停动画（缩放+发光效果）
         - 历史记录项悬停动画（右移效果）
         - 输入框焦点动画
   - **V0.2.2**
      - **新增数据本地存储功能**
         - 数据不会因重启应用和刷新页面而丢失
	 - 结算得分后，数据会自动存储到本地
	 - 每次打开应用时，会自动恢复到上记录的数据
	 - 只有点击重置按钮才能清除数据，重新记录
   - **V0.2.1**
      - **更新桌面应用名字："花牌记分器"**
      - **更新logo**
         - 更新移动端logo
         - 更新web标签logo
      - **添加ios支持**
         - 在ios端，点击分享，添加到桌面即可安装
      - **适配沉浸式主题颜色**
      - **调整界面字体大小**

   - **V0.2**
      - **新的用户界面**
         - 已对多平台设备进行适配(rem+js+vw/vh)
      - **支持基本的PWA功能**
         - 支持离线缓存加速
         - 支持创建桌面应用(支持平台如下：)
            - Android
            - Chrome for Android
            - Chrome for Windows
            - *注：因apple限制，apple设备不支持创建应用*
      - **项目添加部署(Deploy)脚本**
         - 帮助修正本地环境和服务器环境路径问题

   - **V0.1**
      - **完成基本功能**
         - **记分** 、 **结算差错** 、 **重置分数**
